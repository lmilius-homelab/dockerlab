version: '2.2'
services:
  traefik:
    image: traefik:v2.2
    command: --api.insecure=true --providers.docker
    ports:
    - 443:443
    - 8081:8080
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
