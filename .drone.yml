---
kind: pipeline
name: default

trigger:
  branch:
  - master
  event:
  - push

steps:
- name: Deploy container
  image: docker/compose
  commands:
  - docker ps -a
  - docker-compose up -d
  volumes:
  - name: dockersock
    path: /var/run/docker.sock
    
volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock
